// ============================================
// App-weite Konstanten
// ============================================

/** Standard-Zykluslänge (Anzahl Einheiten pro Zyklus) */
export const DEFAULT_CYCLE_LENGTH = 20;

/** Standard-Schwelle für automatische Progression (Einheit) */
export const DEFAULT_PROGRESSION_THRESHOLD = 10;

/** Standard-Gewichtserhöhung in kg */
export const DEFAULT_PROGRESSION_INCREMENT = 2.5;

/** Standard-Pause zwischen Sätzen in Sekunden */
export const DEFAULT_REST_SECONDS = 120;

/** Standard-Sätze pro Übung */
export const DEFAULT_TARGET_SETS = 3;

/** Standard-Wiederholungen pro Satz */
export const DEFAULT_TARGET_REPS = 8;

/** RPE-Bereich */
export const RPE_MIN = 1;
export const RPE_MAX = 10;
export const RPE_STEP = 0.5;

/** Timer-Standardwerte */
export const DEFAULT_TIMER_WORK_SECONDS = 30;
export const DEFAULT_TIMER_REST_SECONDS = 15;
export const DEFAULT_TIMER_INTERVALS = 10;

/** Brand-Farben */
export const ACCENT_COLOR = '#CCFF00'; // Electric Lime
